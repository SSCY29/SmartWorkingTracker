<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SmartWorkingTracker.App.Pages.ContractPage"
             Title="Contratti"
             xmlns:converters="clr-namespace:SmartWorkingTracker.App.Converters">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>

        <VerticalStackLayout Spacing="10" Padding="10" IsVisible="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}">

            <Button Text="➕ Nuovo Contratto"
                Clicked="OnAddClicked"/>

            <CollectionView ItemsSource="{Binding Contracts}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>

                        <Border Padding="10" Margin="5">
                            <Grid ColumnDefinitions="*,Auto">

                                <VerticalStackLayout Grid.Column="0">
                                    <Label Text="{Binding Year}" FontAttributes="Bold"/>
                                    <Label Text="{Binding YearlyLimitHours, StringFormat='Annuale: {0}h'}"/>

                                </VerticalStackLayout>

                                <HorizontalStackLayout Grid.Column="1" Spacing="5">

                                    <Button Text="✏"
                                        WidthRequest="40"
                                        CommandParameter="{Binding}"
                                        Clicked="OnEditClicked"/>
                                    <Button Text="🗑"
                                        WidthRequest="40"
                                        CommandParameter="{Binding}"      
                                        Clicked="OnDeleteClicked"/>

                                </HorizontalStackLayout>

                            </Grid>
                        </Border>

                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>

        <Grid IsVisible="{Binding IsLoading}">
            <VerticalStackLayout VerticalOptions="Center"
                             HorizontalOptions="Center"
                             Spacing="10">

                <ActivityIndicator
            IsRunning="True" />

                <Label Text="Caricamento..."  HorizontalOptions="Center" />

            </VerticalStackLayout>

        </Grid>

    </Grid>
</ContentPage>